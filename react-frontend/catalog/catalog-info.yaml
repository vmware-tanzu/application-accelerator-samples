apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: react-frontend
  description: React Single Page Application with client side rendered web UI. Application provides Service to interact with backend API's.
  tags:
    - react
    - web
    - tanzu
    - typescript
  annotations:
    "backstage.io/kubernetes-label-selector": "app.kubernetes.io/part-of=react-frontend"
spec:
  type: website
  lifecycle: experimental
  owner: default-team
