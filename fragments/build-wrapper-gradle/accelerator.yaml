accelerator:
  displayName: Add Gradle build tool wrapper

  options:
    - name: includeBuildToolWrapper
      inputType: checkbox
      label: Include Build Tool wrapper support
      dataType: boolean
      defaultValue: true
